export type TextFieldProps = {
  label: string;
  name: string;
  type?: string;
  placeholder?: string;
};

export type PasswordFieldProps = {
  label: string;
  name: string;
  type?: string;
  placeholder?: string;
};
